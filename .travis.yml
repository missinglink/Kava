language: php

php:
    - 5.4

before_install:
    - git submodule --quiet update --init --recursive
    - wget http://pecl.php.net/get/pecl_http-2.0.0dev8.tgz
    - tar -xzf pecl_http-2.0.0dev8.tgz
    - sh -c "cd pecl_http-2.0.0dev8 && phpize && ./configure && make && sudo make install"
    - echo "extension=http.so" >> `php --ini | grep "Loaded Configuration" | sed -e "s|.*:\s*||"`

script: phpunit
